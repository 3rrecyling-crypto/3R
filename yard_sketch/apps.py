from django.apps import AppConfig


class YardSketchConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "yard_sketch"
    verbose_name = "Asistente 3D (sketch con IA)"
